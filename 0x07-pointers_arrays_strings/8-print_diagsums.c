#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * print_diagsums - print the sums of diagionals
 *
 * @a: input array
 * @size: size of array
 */
void print_diagsums(int *a, int size)
{
	printf("%d", *a);
	printf("%d", size);
}
