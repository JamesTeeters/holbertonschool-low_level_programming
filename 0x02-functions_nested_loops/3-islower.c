#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int _islower(int c);
	return (0);
}
	int _islower(int c)
{
	int x;

	if (x >= 97 && x <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
