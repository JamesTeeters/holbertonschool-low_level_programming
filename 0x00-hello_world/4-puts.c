#include < studio.h >
#include < string.h >
/**
 * put - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char str1[50];

	strcpy(str1, "\"Programming is like building a multilingual puzzle");

	puts(str1);

	return (0);
}
